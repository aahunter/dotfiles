#!/bin/bash

wrfy="docker run --rm \
                 --interactive \
                 --tty \
                 -v /var/run/docker.sock:/var/run/docker.sock \
                 angrygoat/wrfy:latest"

exec $wrfy $*
