#!/bin/bash
autoflake="docker run --rm \
                    --interactive \
                    --tty \
                    --net "host" \
                    -v "$(pwd):$(pwd)" \
                    -w $(pwd)
                    -it muccg/linter:python3 autoflake"

exec $autoflake $*
