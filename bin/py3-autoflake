#!/bin/bash
autoflake="docker run --rm \
                    --interactive \
                    --tty \
                    -v "$(pwd):$(pwd)" \
                    -w $(pwd)
                    muccg/linter:python3 autoflake"

exec $autoflake $*
