#!/bin/bash
xml2model="docker run --rm \
                   --interactive \
                   --tty \
                   -v "$(pwd):$(pwd)" \
                   -w $(pwd) \
                   muccg/xml2model xml2model"

exec $xml2model "$@"
