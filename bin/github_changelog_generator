#!/bin/bash
github_changelog_generator="docker run --rm \
                    --interactive \
                    --tty \
                    --net "host" \
                    -v "$(pwd):$(pwd)" \
                    -w $(pwd)
                    muccg/github-changelog-generator"

exec $github_changelog_generator "$@"
