#!/bin/bash
pylint="docker run --rm \
                    --interactive \
                    --tty \
                    --net "host" \
                    -v "$(pwd):$(pwd)" \
                    -w $(pwd)
                    -it muccg/linter:python3 pylint"

exec $pylint $*
