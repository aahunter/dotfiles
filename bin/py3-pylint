#!/bin/bash
pylint="docker run --rm \
                    --interactive \
                    --tty \
                    -v "$(pwd):$(pwd)" \
                    -w $(pwd)
                    muccg/linter:python3 pylint"

exec $pylint $*
