#!/bin/bash
autoflake="docker run --rm \
                    --interactive \
                    --tty \
                    -v "$(pwd):$(pwd)" \
                    -w $(pwd)
                    muccg/linter:python2 autoflake"

exec $autoflake $*
