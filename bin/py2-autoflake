#!/bin/bash
autoflake="docker run --rm \
                    --interactive \
                    --tty \
                    -v "$(pwd):$(pwd)" \
                    -w $(pwd)
                    -it muccg/linter:python2 autoflake"

exec $autoflake $*
