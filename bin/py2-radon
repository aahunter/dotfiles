#!/bin/bash
radon="docker run --rm \
                    --interactive \
                    --tty \
                    -v "$(pwd):$(pwd)" \
                    -w $(pwd)
                    -it muccg/linter:python2 radon"

exec $radon $*
