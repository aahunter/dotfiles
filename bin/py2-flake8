#!/bin/bash
flake8="docker run --rm \
                    --interactive \
                    --tty \
                    -v "$(pwd):$(pwd)" \
                    -w $(pwd)
                    -it muccg/linter:python2 flake8"

exec $flake8 $*
