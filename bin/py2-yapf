#!/bin/bash
yapf="docker run --rm \
                    --interactive \
                    --tty \
                    -v "$(pwd):$(pwd)" \
                    -w $(pwd)
                    muccg/linter:python2 yapf"

exec $yapf $*
