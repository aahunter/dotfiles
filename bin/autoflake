#!/bin/bash
autoflake="docker run --rm \
                    --interactive \
                    --tty \
                    --net "host" \
                    -v /var/run/docker.sock:/var/run/docker.sock \
                    -v "$(pwd):$(pwd)" \
                    -w $(pwd)
                    -it muccg/linter autoflake"

exec $autoflake $*
