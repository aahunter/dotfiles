#!/bin/bash
autoflake="docker run --rm \
                    --interactive \
                    --tty \
                    --net "host" \
                    -v "$(pwd):$(pwd)" \
                    -w $(pwd)
                    -it muccg/linter autoflake"

exec $autoflake $*
