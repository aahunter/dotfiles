#!/bin/bash
gjslint="docker run --rm \
                    --interactive \
                    --tty \
                    -v "$(pwd):$(pwd)" \
                    -w $(pwd)
                    muccg/linter:python3 gjslint"

exec $gjslint $*
