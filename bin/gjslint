#!/bin/bash
gjslint="docker run --rm \
                    --interactive \
                    --tty \
                    --net "host" \
                    -v "$(pwd):$(pwd)" \
                    -w $(pwd)
                    -it muccg/linter gjslint"

exec $gjslint $*
