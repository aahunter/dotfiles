#!/bin/bash
autopep8="docker run --rm \
                    --interactive \
                    --tty \
                    -v "$(pwd):$(pwd)" \
                    -w $(pwd)
                    muccg/linter:python2 autopep8"

exec $autopep8 $*
